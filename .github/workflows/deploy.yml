name: Deploy Traefik to Debian 12

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      name: Deploy Traefik Local

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Déployer l'application localement
        run: |
          docker compose down
          docker compose up -d
